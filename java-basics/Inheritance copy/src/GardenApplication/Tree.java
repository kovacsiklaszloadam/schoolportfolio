package GardenApplication;

public class Tree {

    static int waterAmount;
    static String name;

    Tree(int waterAmount) {
        this.name = name;
        this.waterAmount = 0;
        Garden.objectCount++;

    }

    static String needsWater = (waterAmount < 10) ? "Tree needs water." : "Tree doesn't need water.";
}
